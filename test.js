 const createElements = (array)=>{
    const createHtml = `<span>${array}</span>`;
    console.log(createHtml)
 }
 
 
 const labels = [
        "documentation",
        "good first issue"
      ]
createElements(labels)